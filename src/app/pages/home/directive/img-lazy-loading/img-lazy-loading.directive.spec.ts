import { ImgLazyLoadingDirective } from './img-lazy-loading.directive';

describe('ImgLazyLoadingDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgLazyLoadingDirective();
    expect(directive).toBeTruthy();
  });
});
