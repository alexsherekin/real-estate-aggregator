import { BackgroundImageLazyLoadingDirective } from './background-image-lazy-loading.directive';

describe('BackgroundImageLazyLoadingDirective', () => {
  it('should create an instance', () => {
    const directive = new BackgroundImageLazyLoadingDirective();
    expect(directive).toBeTruthy();
  });
});
